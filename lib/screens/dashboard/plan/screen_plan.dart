part of '../../_screens.dart';

@RoutePage()
class ScreenPlan extends StatelessWidget {
  const ScreenPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Screen Plan Work Ongoing"),
      ),
    );
  }
}
